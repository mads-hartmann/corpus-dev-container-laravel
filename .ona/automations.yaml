tasks:
  installDeps:
    name: Install dependencies
    description: Install PHP and Node dependencies, prepare the environment file, app key, and SQLite database.
    command: |
      composer install
      [ -f .env ] || cp .env.example .env
      php artisan key:generate
      [ -f database/database.sqlite ] || touch database/database.sqlite
      php artisan migrate --force
      npm install
    triggeredBy:
      - manual
      - postDevcontainerStart
      - prebuild

  build:
    name: Build assets
    description: Build the frontend assets with Vite.
    command: |
      npm run build
    triggeredBy:
      - manual
    dependsOn:
      - installDeps

  test:
    name: Run tests
    description: Run the PHPUnit test suite.
    command: |
      php artisan config:clear --ansi
      php artisan test
    triggeredBy:
      - manual
    dependsOn:
      - installDeps

services:
  app:
    name: Laravel App
    description: Laravel development server.
    triggeredBy:
      - manual
      - postEnvironmentStart
    commands:
      start: |
        ona environment port open 8000 --dont-wait --admission creator_only --name "Laravel App"
        php artisan serve --host 0.0.0.0 --port 8000
      ready: |
        curl -sf http://localhost:8000 > /dev/null

  vite:
    name: Vite Dev Server
    description: Vite dev server for hot-reloading frontend assets.
    triggeredBy:
      - manual
      - postEnvironmentStart
    commands:
      start: |
        ona environment port open 5173 --dont-wait --admission creator_only --name "Vite Dev Server"
        # Resolve the public host Ona forwards 5173 to, so Vite accepts the
        # Host header and emits asset/HMR URLs the browser can actually reach.
        until ONA_VITE_HOST="$(ona environment port get 5173 -f URL 2>/dev/null | cut -c 9- | sed 's:/*$::')" && [ -n "$ONA_VITE_HOST" ]; do
          echo "Waiting for forwarded host for port 5173..."
          sleep 1
        done
        export ONA_VITE_HOST
        echo "Vite allowed host: $ONA_VITE_HOST"
        npm run dev -- --host 0.0.0.0
      ready: |
        curl -s -o /dev/null http://localhost:5173
